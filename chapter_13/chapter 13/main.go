package main

const userName = "kiarash"
const age = 60 / 2

// const random = rand.Int() // not allowed

const (
	inputAttack        = iota + 1
	inputSpecialAttack = iota + 10
	inputHeal
)

func main() {
	// userName = "KiarashAmiri"
}
